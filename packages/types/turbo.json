{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "package.json",
        "src/**/*.ts",
        "src/**/*.tsx"
      ],
      "outputs": ["lib/**"]
    }
  }
}
