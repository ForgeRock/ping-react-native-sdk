---
'@ping-identity/rn-journey': minor
---

feat(rn-journey): Add AM/AIC transactional backchannel authentication via `client.startBackchannel(redirectUri)` and the `useJourney` `startBackchannel` action (SDKS-5380).
